[project]
name = "fpga-nn"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Henrik Thoroe", email = "uiqwd@student.kit.edu"},
]
dependencies = []
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
